"""DAY 5 — LangChain tools available to the agent."""
# TODO D5: @tool search_docs(query: str, section: str = None) -> str
#            calls retriever.retrieve(), formats chunks as numbered context
# TODO D5: @tool lookup_glossary(term: str) -> str
#            looks up term in data/glossary.json, returns aliases
# TODO D5: @tool create_handoff(reason: str) -> str
#            logs handoff request, returns handoff message to user
